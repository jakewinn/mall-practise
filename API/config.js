let config = {
    host: 'localhost',
    port:3306,
    user: 'root',
    password: 'wcx',
    database: 'no_mall',
    // timezone: "SYSTEM",
    // connectionLimit: 10 // 控制连接池的大小为 10 个连接
}

module.exports = config;